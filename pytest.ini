[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    e2e: end-to-end tests requiring a running server and Playwright (skip with -m "not e2e")
