[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -m "not e2e"
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    e2e: end-to-end tests requiring a running server and Playwright (run with -m e2e)
